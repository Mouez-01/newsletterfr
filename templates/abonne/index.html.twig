{% extends 'base.html.twig' %} {% block title %}Abonne index{% endblock %} {% block body %}
<h1 class="mt-2">Abonne index</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nom</th>
            <th>Email</th>
            <th>Autorisation</th>
            <th>Commentaire</th>
            <th>Action</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for abonne in abonnes %}
        <tr>
            <td>{{ abonne.id }}</td>
            <td>{{ abonne.nom }}</td>
            <td>{{ abonne.email }}</td>
            <td>{{ abonne.autorisation }}</td>
            <td>{{ abonne.commentaire }}</td>
            <td>{{ abonne.action }}</td>
            <td>
                <a class="btn btn-lg btn-info btn-block mt-3" href="{{ path('abonne_show', {'id': abonne.id}) }}">Afficher </a>
                <a class="btn btn-lg btn-info btn-block mt-3" href="{{ path('abonne_edit', {'id': abonne.id}) }}">Modifier</a>
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="7">Aucun enregistrement trouvé</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

<a class="btn btn-lg btn-info btn-block mt-3" href="{{ path('abonne_new') }}">Create new</a> {% endblock %}