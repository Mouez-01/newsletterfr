{% extends 'base.html.twig' %} {% block title %}Abonne{% endblock %} {% block body %}
<h1>Abonne</h1>

<table class="table">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ abonne.id }}</td>
        </tr>
        <tr>
            <th>Nom</th>
            <td>{{ abonne.nom }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ abonne.email }}</td>
        </tr>
        <tr>
            <th>Autorisation</th>
            <td>{{ abonne.autorisation }}</td>
        </tr>
        <tr>
            <th>Commentaire</th>
            <td>{{ abonne.commentaire }}</td>
        </tr>
        <tr>
            <th>Action</th>
            <td>{{ abonne.action }}</td>
        </tr>
    </tbody>
</table>

<a class="btn btn-lg btn-info btn-block mt-3" href="{{ path('abonne_index') }}">back to list</a>

<a href="{{ path('abonne_edit', {'id': abonne.id}) }}">edit</a> {{ include('abonne/_delete_form.html.twig') }} {% endblock %}